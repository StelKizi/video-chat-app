import React from 'react';
import Notifications from './Notifications';

const Options = () => {
  return (
    <div>
      Options
      <Notifications />
    </div>
  );
};

export default Options;
